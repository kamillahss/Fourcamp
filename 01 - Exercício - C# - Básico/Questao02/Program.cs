// Leia dois valores inteiros, no caso para variáveis “num1” e “num2”. A seguir, calcule a soma entre elas e atribua à variável “soma”. A seguir escrever o valor desta variável

int num1, num2, soma;

num1 = Convert.ToInt32(Console.ReadLine());
num2 = Convert.ToInt32(Console.ReadLine());

soma = num1 + num2;
Console.WriteLine("Soma: " + soma);